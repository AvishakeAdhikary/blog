'use client';

// This file exists for completeness; copy-code logic is injected at runtime by MarkdownContent.
// Kept as a no-op component to satisfy the component tree described in the plan.
export function CopyCodeButton() {
  return null;
}
